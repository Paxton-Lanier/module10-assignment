# module10-assignment
In this repository we contain the assignment known as module 10. We applied type conversions to ensure proper data formats using .astype(), pd.to_numeric(), and pd.to_datetime().
